name: Update Site

on:
  schedule:
    - cron: "0 16 * * *"

  workflow_dispatch:

jobs:
  updateSite:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          path: carlinmack.github.io

      - name: Set up Python 3.8
        uses: actions/setup-python@v2
        with:
          python-version: 3.8

      - name: Install dependencies
        run: |
          cd carlinmack.github.io/.github
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Check if metadata has been modified
        run: |
          cd carlinmack.github.io/.github
          python checkMetadata.py

      - name: Checkout site generation repository
        uses: actions/checkout@v2
        with:
          repository: carlinmack/afp
          token: ${{ secrets.AFP_PAT }}
          path: afp

      - name: Download metadata/ and thys/
        run: |
          wget https://foss.heptapod.net/isa-afp/afp-devel/-/archive/branch/default/afp-devel-branch-default.tar.gz?path=thys -O thys.tar.gz &&
          tar -xf thys.tar.gz
          wget https://foss.heptapod.net/isa-afp/afp-devel/-/raw/branch/default/metadata/metadata

      - name: Overwrite repo files
        run: |
          rm -rf afp/src/afp-devel/thys
          mv afp-devel-branch-default-thys/thys afp/src/afp-devel/
          mv -f metadata afp/src/afp-devel/metadata/metadata

      - name: Commit changes
        uses: EndBug/add-and-commit@v5
        with:
          author_name: github-actions
          author_email: 41898282+github-actions[bot]@users.noreply.github.com
          message: "Update metadata and thys"
          branch: "master"
          cwd: "afp"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install dependencies
        run: |
          cd afp/src/afp-devel/admin
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Set up Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: "latest"
          extended: true

      - name: Build
        run: |
          cd afp/src/afp-devel/admin/hugo
          hugo --minify --destination ~/carlinmack.github.io/

      - name: Reverse SHA
        run: rev .github/Last-Modified-SHA.txt > .github/Last-Modified-SHA.txt

      - name: Commit changes
        uses: EndBug/add-and-commit@v5
        with:
          author_name: github-actions
          author_email: 41898282+github-actions[bot]@users.noreply.github.com
          message: "Update site"
          cwd: "carlinmack.github.io"
          branch: "main"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
